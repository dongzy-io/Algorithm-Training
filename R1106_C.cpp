//TO BE CONTINUED
#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int T;
    cin>>T;
    while(T--){
        int n;
        cin>>n;
        vector<int> fa(n+1);
        vector<vector<int>> height(n+1);
        for(int i=2;i<=n;i++){
            int x;
            cin>>x;
            fa[i]=x;
            height[height[x]+1].emlace_ba
        }
    }
}