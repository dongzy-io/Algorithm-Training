#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    string s;
    cin>>s;
    if(s=="MATIJI"||s=="matiji")
        cout<<"Yes";
    else
        cout<<"No";
    return 0;
}